import { WelcomeTitle } from "../Components/WelcomeTitle/WelcomeTitle"

export const Home = () => {
    return(
        <WelcomeTitle>
            Bem-vindo(a), Usuário(a)
        </WelcomeTitle>
    )
}